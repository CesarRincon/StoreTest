import { StyleSheet } from 'react-native';

export const homeStyles = StyleSheet.create({
    scrollView: {
        flex: 1,
        height: "100%",
        paddingHorizontal: 10,
        width: "100%"
    }
});