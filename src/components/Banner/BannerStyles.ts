import { StyleSheet } from 'react-native';

export const bannerStyles = StyleSheet.create({
    container: {
        height: 120,
        marginTop: 10,
        justifyContent: "center",
        alignItems: "center",
        overflow: "hidden"
    }
});